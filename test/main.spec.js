describe('main', () => {
    it('debería ser una funcion', () => {
        assert.isFunction(intro);
    });
    it('debería ser una funcion', () => {
        assert.isFunction(btnRegister);
    });
    it('debería ser una funcion', () => {
        assert.isFunction(btnAdmin);
    });
    it('debería ser una funcion', () => {
        assert.isFunction(createVisitorRecord);
    });
});